# Database Notes

- SQL schema is defined in `schema.sql`.
- Runtime database is SQLite (`support_system.db`) and is created automatically by SQLAlchemy.
- Root `requirements.txt` is the primary dependency file for the full project.
